<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="android.view.View" />

        <variable
            name="portrait"
            type="berlin.mfn.naturblick.room.FullPortrait" />

        <variable
            name="species"
            type="berlin.mfn.naturblick.room.Species" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/secondary_500_opacity_10">

        <ImageView
            android:id="@+id/species_image"
            android:layout_width="@dimen/avatar_size"
            android:layout_height="@dimen/avatar_size"
            android:layout_marginHorizontal="@dimen/default_margin"
            android:layout_marginTop="@dimen/default_margin"
            app:imageUrlThumbnailCrop="@{species.avatarUrl}"
            app:layout_constraintBottom_toTopOf="@id/image_or_name"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintVertical_bias="0" />

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/sciname"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="@dimen/default_margin"
            android:layout_marginTop="@dimen/avatar_text_offset"
            android:text="@{species.sciname}"
            android:textAppearance="?attr/textAppearanceSubtitle3"
            android:textColor="@color/sunset"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0"
            app:layout_constraintVertical_bias="0"
            app:layout_constraintStart_toEndOf="@id/species_image"
            app:layout_constraintTop_toTopOf="@id/species_image" />

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/name"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="@dimen/default_margin"
            android:layout_marginBottom="@dimen/avatar_text_offset"
            android:text="@{species.name}"
            android:textAppearance="?attr/textAppearanceSubtitle1"
            android:textColor="@color/black"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0"
            app:layout_constraintVertical_bias="0"
            app:layout_constraintStart_toEndOf="@id/species_image"
            app:layout_constraintTop_toBottomOf="@id/sciname"
            app:layout_constraintBottom_toBottomOf="@id/image_or_name"/>

        <androidx.constraintlayout.widget.Barrier
            android:id="@+id/image_or_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:barrierDirection="bottom"
            app:constraint_referenced_ids="species_image,name" />

        <berlin.mfn.naturblick.ui.species.portrait.UnambiguousFeatureList
            android:id="@+id/feature_list"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/default_margin"
            android:orientation="vertical"
            app:features="@{portrait.unambiguousFeatures}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/image_or_name" />

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/similar_species"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginHorizontal="@dimen/default_margin"
            android:layout_marginBottom="@dimen/default_margin"
            android:text="@string/similar_species"
            android:textAppearance="?attr/textAppearanceHeadline3"
            android:visibility="@{portrait.similarSpecies.isEmpty() ? View.GONE : View.VISIBLE}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/feature_list" />

        <berlin.mfn.naturblick.ui.species.portrait.SimilarSpeciesList
            android:id="@+id/portrait_similar_species"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingHorizontal="@dimen/default_margin"
            android:paddingBottom="@dimen/default_margin"
            android:visibility="@{portrait.similarSpecies.isEmpty() ? View.GONE : View.VISIBLE}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/similar_species"
            app:similarSpecies="@{portrait.similarSpecies}" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>